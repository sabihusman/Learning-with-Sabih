import LinkedListArrayViz from '@/app/components/LinkedListArrayViz'

export const metadata = {
  title: 'Linked list vs array - Learning with Sabih',
}

# Linked list vs array

An array stores its values in one contiguous block with numbered slots, while a linked list strings its values together with pointers from one node to the next. That difference changes what each operation costs. The figure below runs the same operation on both structures and counts the real work each one does.

<LinkedListArrayViz />
