import ReplicationViz from '@/app/components/ReplicationViz'

export const metadata = {
  title: 'Replication - Learning with Sabih',
}

# Replication

{/* TODO: placeholder prose. The real prose is supplied in chat after the owner reviews the reported operation stream, lags, and kill outcomes, and lands on this branch before merge. Honesty constraints for the prose: the twenty-operation stream, the two replica lags (2 and 4 steps), and the starting balance are authored and frozen; every balance, arrival step, pending list, stale-read count, and loss set is computed from the step index, never typed in; there is no clock, no randomness, and no network model, so "lag" here is a fixed number of steps and not a latency; synchronous mode charges the write the full worst-case lag, which is why the same twenty operations take more steps; a write is acknowledged at issue in asynchronous mode and only on full replication in synchronous mode, which is the entire difference between losing acknowledged writes and losing only in-flight ones on failover; the figure promotes the replica that is furthest ahead and discards the old primary, where real systems use quorums rather than all-replica acknowledgement, replicate over a network whose delay varies, and can often recover a failed primary instead of throwing it away; no durability, availability, or performance claim about any real database. */}

Placeholder: one account balance is copied from a primary onto two replicas that each receive writes after a fixed lag, so reads routed to a replica can return an old value, and killing the primary can strand the writes that had not arrived yet.

## Watching the lag

<ReplicationViz />
