import IsolationLevelsViz from '@/app/components/IsolationLevelsViz'

export const metadata = {
  title: 'Isolation levels - Learning with Sabih',
}

# Isolation levels

Placeholder: this is a temporary paragraph so the topic builds and renders. The real prose is applied in a later step. Isolation levels control which concurrency phenomena a transaction can see. Turn the dial and pick a phenomenon in the figure below to watch how PostgreSQL behaves at each level.

<IsolationLevelsViz />
