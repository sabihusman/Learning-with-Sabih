import BroadcastingViz from '@/app/components/BroadcastingViz'

export const metadata = {
  title: 'Broadcasting - Learning with Sabih',
}

# Broadcasting

PLACEHOLDER: Broadcasting is how an operation combines two arrays of different
shapes by stretching size-1 or missing dimensions to match, without copying
data. This is a placeholder paragraph; the real prose is a later step. Pick two
shapes and read the alignment from the right to see whether they combine.

<BroadcastingViz />
