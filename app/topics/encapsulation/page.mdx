import EncapsulationViz from '@/app/components/EncapsulationViz'

export const metadata = {
  title: 'Encapsulation - Learning with Sabih',
}

# Encapsulation

TODO: Placeholder prose. This paragraph will be replaced with the finalized copy in a follow-up. It introduces the idea that an object has rules about its own state, called invariants, that must always hold.

TODO: Placeholder prose. The second paragraph will explain that a public field lets any outside code write whatever it wants, which can put the object into an invalid state that breaks an invariant.

TODO: Placeholder prose. The final paragraph will set up the figure below, where making the field private and going through a method lets the object refuse writes that would break its rule.

<EncapsulationViz />
