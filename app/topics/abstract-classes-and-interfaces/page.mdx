import AbstractInterfaceViz from '@/app/components/AbstractInterfaceViz'

export const metadata = {
  title: 'Abstract classes and interfaces - Learning with Sabih',
}

# Abstract classes and interfaces

TODO: Placeholder prose. This paragraph will be replaced with the finalized copy in a follow-up. It introduces the idea of a contract: a type that forces the classes below it to provide certain methods.

TODO: Placeholder prose. The second paragraph will contrast the two kinds of contract, an abstract class that can also carry state and implemented behavior, and an interface that mainly specifies what must be provided.

TODO: Placeholder prose. The final paragraph will set up the figure below, where a Robot type is shown as each kind of contract and a checklist compares what each one can and cannot carry.

<AbstractInterfaceViz />
