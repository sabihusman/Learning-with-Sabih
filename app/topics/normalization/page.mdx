import NormalizationViz from '@/app/components/NormalizationViz'

export const metadata = {
  title: 'Normalization - Learning with Sabih',
}

# Normalization

TODO: Placeholder prose. This paragraph will be replaced with the finalized copy in a follow-up. It introduces the idea that stuffing everything into one wide table causes the same fact to be stored in many places.

TODO: Placeholder prose. The second paragraph will walk from the messy table through first normal form (atomic cells) to a split that puts each fact in one place, and will name the update anomaly that redundancy causes.

TODO: Placeholder prose (final prose must note this honestly): the figure goes from first normal form straight to third normal form and deliberately skips a separate second-normal-form step, because the single split shown resolves both the partial and the transitive dependency for this table and a 2NF-only intermediate would add a step without adding insight.

<NormalizationViz />
