import ShardingViz from '@/app/components/ShardingViz'

export const metadata = {
  title: 'Sharding - Learning with Sabih',
}

# Sharding

{/*
  TODO: final prose lands in a follow-up.

  Honesty constraints the prose must honor:
  - The hash is a real FNV-1a (32-bit) hash: offset basis 2166136261, prime
    16777619, XOR each byte then multiply. Placement = fnv1a(String(value)) mod
    the shard count. This is genuinely computed, not choreographed.
  - The dataset is 14 hand-authored rows, small on purpose so the skew is easy
    to see. Real shards hold millions of rows, and real deployments use far
    more than 3 or 4 shards.
  - Measured per-shard counts (verified against the committed code, not
    estimated):
    - key = id (high cardinality, unique per row):
      - 3 shards: [7, 4, 3]
      - 4 shards: [3, 4, 3, 4]
      id spreads reasonably across shards at both shard counts.
    - key = country (7 distinct values, mildly uneven: DE x3, CA x3, FI x2,
      JP x2, AR x2, MX x1, IE x1):
      - 3 shards: [4, 5, 5]
      - 4 shards: [3, 3, 5, 3]
      country spreads mildly across every shard at both shard counts, no shard
      holding close to half the rows. This is a real, measured outcome of
      hashing a medium-cardinality column, not a staged result.
    - key = plan (2 distinct values, skewed: free x11, pro x3):
      - 3 shards: [14, 0, 0] (every row lands in a single shard)
      - 4 shards: [3, 0, 0, 11]
      plan produces the clearest hot shard: a column with only two possible
      values can never occupy more than two shards, no matter how many shards
      exist, and the skew toward "free" concentrates the load further.
  - The UI defaults to key = id, shards = 4, which shows the most balanced
    view first; switching to plan reveals a hot shard, and switching to
    country shows a mild, in-between spread.
  - Core lesson: the partition key's cardinality caps how many shards can ever
    be used, and skew within that key decides how unevenly load piles onto
    the shards that are used. Adding more shards does not fix a low-cardinality
    key. country (medium cardinality) demonstrates the middle ground between
    id's even spread and plan's hot shard.
  - Honesty note (must appear, substantially as follows): this is a real hash
    over a tiny, hand-picked dataset built to make the lesson visible. Real
    sharded systems pick partition keys deliberately to avoid this failure
    mode, often combining a high-cardinality key with the low-cardinality one,
    or hashing a composite key.
  - No-subhead / figure-last structure, matching the other section pages.
    Straight quotes and apostrophes only, no em or en dashes.
*/}

Placeholder prose. The interactive figure below is complete; the written
explanation will be added in a follow-up.

<ShardingViz />
