import SortingViz from '@/app/components/SortingViz'

export const metadata = {
  title: 'Sorting - Learning with Sabih',
}

# Sorting

Sorting puts a collection in order, and different algorithms reach the same result with very different amounts of work. The figure below runs three real sorts on one fixed array so you can step through each and compare how many comparisons and moves they take.

<SortingViz />
