import ConstructorsHeapViz from '@/app/components/ConstructorsHeapViz'

export const metadata = {
  title: 'Constructors and the heap - Learning with Sabih',
}

# Constructors and the heap

TODO: Placeholder prose. This paragraph will be replaced with the finalized copy in a follow-up. It introduces the idea that a variable does not hold an object directly; it holds a reference to an object that lives on the heap.

TODO: Placeholder prose. The second paragraph will cover what `new` and the constructor actually do: allocate space on the heap and initialize the object's fields, with the variable ending up holding a reference to it.

TODO: Placeholder prose. The final paragraph will set up the figure below, where copying a reference makes two variables name the same object, so a change through one is visible through the other.

<ConstructorsHeapViz />
