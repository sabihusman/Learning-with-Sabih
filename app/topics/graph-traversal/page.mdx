import GraphTraversalViz from '@/app/components/GraphTraversalViz'

export const metadata = {
  title: 'Graph traversal - Learning with Sabih',
}

# Graph traversal

A graph is a set of nodes joined by edges, and traversal means visiting every node by following those edges. The two classic ways to do it, breadth-first search and depth-first search, differ in only one thing: whether the nodes waiting to be visited are held in a queue or a stack. The figure below runs both on the same graph so you can watch the visit order and the data structure that drives it.

<GraphTraversalViz />
