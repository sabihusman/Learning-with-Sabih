import DnsViz from '@/app/components/DnsViz'

export const metadata = {
  title: 'DNS - Learning with Sabih',
}

# DNS

{/*
  TODO: final prose lands in a follow-up.

  Honesty constraints the prose must honor:
  - Verified against RFC 1034 (query resolution, recursive vs iterative
    queries, referrals, caching/TTL) and IANA's root server documentation
    (root servers as the resolver's bootstrap entry point). No deviation from
    current docs was found; state the chain plainly, not as folklore.
  - The chain: stub resolver (the device) sends ONE recursive query to the
    recursive resolver and waits. The resolver does the iterative work:
    root, then the .com TLD, then the authoritative server for example.com.
  - Root and TLD servers NEVER hold the final answer. They reply with a
    REFERRAL (who to ask next), not an answer. Only the authoritative server
    holds the actual record. This is the core point and must survive into
    prose, not get softened into "they look it up."
  - The example domain is app.example.com (example.com is reserved for
    documentation, RFC 2606). The example IP is 192.0.2.42, from the
    documentation-only TEST-NET-1 range (RFC 5737). Say both are reserved,
    not real.
  - The resolver CACHES the authoritative answer (with its TTL, 300 seconds
    here) the moment it arrives, before relaying it to the device.
  - First lookup (cold, empty cache): 4 servers asked (device to resolver,
    resolver to root, resolver to TLD, resolver to auth), 8 steps total
    (each server pair does a query then a reply).
  - Second lookup, run immediately after with the cache still warm: 1 server
    asked (just the device asking the resolver), 2 steps, answer returned
    immediately. Root, TLD, and the authoritative server are never contacted
    and stay visually dark in the figure.
  - An "Expire cache" control simulates the TTL aging out; the lookup after
    that walks the full chain again exactly like the first cold run.
  - The point is that distributing the answer this way plus caching is what
    makes near-instant lookups the common case, not the exception. Root and
    TLD servers handle enormous query volume precisely because they only ever
    hand out referrals, which are small and reusable, not authoritative
    answers for every domain on earth.
  - Simplifications to state plainly: one query type (an A record lookup),
    one resolver, no device or browser cache shown (only the resolver's),
    root/TLD/auth each stand in for many real server instances, no CNAME
    chains.
  - No-subhead / figure-last structure, matching the other section pages.
    Straight quotes and apostrophes only, no em or en dashes.
*/}

Placeholder prose. The interactive figure below is complete; the written
explanation will be added in a follow-up.

<DnsViz />
